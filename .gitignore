.claude/
.DS_Store

# Environment files with secrets loaded by spring-dotenv — never commit real values.
.env
**/.env
!**/.env.example

# Stray lock/temp artifacts left behind on the SMB network share by Maven/npm
# file-locking (garbled dotfile names, e.g. ".<junk>0005000000<hex>")
**/.[!a-zA-Z]*
